<% flash.each do |type, message| %>
  <div class="mb-4 rounded-lg border px-4 py-3 text-sm <%= type.to_s == "alert" ? "border-red-300 bg-red-50 text-red-700" : "border-green-300 bg-green-50 text-green-700" %>">
    <%= message %>
  </div>
<% end %>
